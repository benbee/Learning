#include <sys/types.h>
#include <unistd.h>
#include <stdio.h>

void do_something(long t)
{
	int i = 0;
	for (i = i; i < t; i++) {
		for (i = 0; i < t; i++) {
			for (i = 0; i < t; i++) {
			}
		}
	}
}

int main(int argc, char *argv[])
{
    pid_t t;

	/*  */
	printf("PID before fork(): %d\n", getpid());
	pid = fork();

	pid_
    return 0;
}
